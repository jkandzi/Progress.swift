language: objective-c
os: osx
osx_image: xcode7.2

xcode_project: Progress.swift/Progress.swift.xcodeproj
xcode_scheme: Progress.swift

script: xcodebuild -project Progress.swift/Progress.swift.xcodeproj -scheme 'Progress.swift' clean build test GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES | xcpretty

after_success:
- bash <(curl -s https://codecov.io/bash)
